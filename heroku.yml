build:
  docker:
    release:
      dockerfile: Dockerfile
      target: production
      worker: worker/Dockerfile
release:
  image: worker
run:
  web: npm start
  worker:
    image: web
    # web: Dockerfile
