build:
  docker:
    worker: worker/Dockerfile
    release:
      dockerfile: Dockerfile
      target: production
release:
  image: worker
run:
  web: npm start
  worker:
    image: web
    # web: Dockerfile
